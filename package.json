{
  "name": "wp-campsite-checker",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter frontend dev",
    "build": "pnpm --filter frontend build",
    "deploy:backend": "pnpm --filter backend deploy",
    "deploy:frontend": "pnpm --filter frontend build && aws s3 sync frontend/dist/ s3://$(cd backend && serverless info --verbose 2>/dev/null | grep FrontendBucket | awk '{print $2}') --delete"
  }
}
